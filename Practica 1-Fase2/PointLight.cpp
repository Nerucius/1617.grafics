#include "PointLight.h"

PointLight::PointLight(/*vec3 pos, vec3 amb, vec3 dif, vec3 spec*/){
    /*
    position = pos;
    color = color;
    ambient = amb;
    diffuse = dif;
    specular = spec;
    */
}
